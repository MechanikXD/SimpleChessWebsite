const config = {
    draggable: true,
    dropOffBoard: 'snapback',
    position: 'start',
    moveSpeed: 'slow',
    snapbackSpeed: 500,
    snapSpeed: 100,
}

const board1 = ChessBoard('myBoard', config);